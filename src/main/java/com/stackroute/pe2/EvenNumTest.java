package com.stackroute.pe2;

public class EvenNumTest {
    public static boolean isEven(int number){//Function with boolean return type

        //Checking whether the number odd or even
        if(number%2==0)//If number is even
        {
            return true;
        }else{//If number is odd
            return false;
        }
    }
}
